[project]
name = "spyro-launcher"
version = "0.1.0"
description = "Locked bootstrap environment for the Spyro native port"
requires-python = ">=3.11"
dependencies = [
    "capstone>=5,<6",
]

[tool.uv]
package = false
