---
id: I053
kind: instrument
status: trusted
created: 2026-08-21
---

## Instrument

./run.sh -> tools/run.py launcher

## Validated by

On 2026-08-21, an explicit nonexistent CHD refused with exit 1 after framework/submodule resolution and before any CMake build or launch. The true zero-argument route then resolved the configured Spyro disc, provisioned the current recomp, configured both C++ builds as Clang, launched scratch/bin/spyro_port on the native renderer, and exited cleanly at a 400-presented-frame cap. After establishing the Clang selection marker, two consecutive no-argument runs retained both caches; the second reported recomp up to date and exited in 2.1 seconds without a fresh configure or recompilation. tests/test_launcher.py independently exercises sequence ordering, target identity, refusal ordering, and launch-environment preservation.

## Known failure modes

(none recorded yet)
