---
id: 58
title: The producer DB emits NOTHING in this port — the census lifecycle was unreachable, and it fails SILENTLY
status: open
symptom: no [producers] run-end line, no scratch/producers/ directory, no claims.txt, even though the census is armed and fed
tags: producers,census,verification,instrument
created: 2026-08-12
updated: 2026-08-12
---

MEASURED 2026-08-12 by adversarial verification of a claim I had made in the pin-bump commit ('bumping is all this repo needs'). Three separate things are missing, in this order:

(a) THE LIFECYCLE WAS UNREACHABLE. The DB's load/report/persist calls lived inside game_main in the framework's native_boot.cpp, and THIS PORT NEVER REACHES IT: game/core/main.cpp calls dc_boot_init and never native_boot_run, and game/core/frame_loop.cpp run() is [[noreturn]] with an unconditional for(;;). Verified: scratch/producers/ does not exist in this repo after a 90s headless run, while Tomba2Engine/scratch/producers/ holds today's runs as the positive control. FIXED FRAMEWORK-SIDE (psxport 240d8f9a): producer_db_begin(c) / producer_db_finish(c) in producer_db.h are now port-callable. THIS PORT STILL HAS TO CALL THEM — two lines, in frame_loop.cpp around the loop. Until it does, the DB produces nothing here.

(b) THE GUEST LEG IS STRUCTURALLY BLIND. game/core/game_config.cpp:79-80 has packetPoolBase = 0, packetPoolStride = 0, so pool_range() returns known=false and OtAttr records no spans. The framework DOES say so out loud ('packet-pool attribution is STRUCTURALLY BLIND here'), which is the one part of this that was never silent. Un-blinding it needs the packet pool RE'd.

(c) NO NATIVE PRODUCER IS DECLARED. Zero hits for ProducerScope / pc_producer anywhere in this tree, and the native render-queue chokepoint has exactly ONE game call site (game/render/fx_title_menu.cpp:215 push2dQuad); game/core/vsync.cpp:229 says in its own comment that this repo never calls drawWorldQuad. Measured with PSXPORT_DEBUG=unscoped over a 75s run: 0 undeclared-native-prim lines, i.e. the native leg pushed nothing through the chokepoint at all.

ORDER MATTERS: (a) first, or the other two cannot be observed — with no report there is no way to see that the guest leg is blind or that no producer is declared. Do NOT quote 'the framework does this automatically' for this port until (a) is called here.

### Note (2026-08-12)
PART (a) FIXED IN THIS PORT, MEASURED 2026-08-12; (b) and (c) still open, which is why this entry stays open. game/core/producer_run.{h,cpp} now owns the DB lifecycle: producer_db_begin(c) from main.cpp before dc_boot_init, and producer_db_finish(c) driven from vsync.cpp's vblank field — the port's only real frame boundary, the same place the render-queue flush, present, pace, audio and event delivery already live. THE MISSING 'AFTER THE LAST FRAME' IS MADE, NOT FOUND: PSXPORT_NATIVE_FRAMES=<n> (the framework's own headless cap knob, dead code in this port until now; same default 0=uncapped; one count per presented frame, because one delivered field is one present here) ends the run at frame n, writes the DB and exits 0. atexit is the secondary path, so a window-close session also emits. REJECTED WITH REASONS: a PERIODIC FLUSH would double-count — ProducerCensus::appendClaims appends mClaimCount addresses to claims.txt on every call with no dedup, and the per-run JSONL name is only second-resolution, so N flushes inflate the next run's claim set N-fold; a SIGNAL PATH cannot work — watchdog.cpp owns SIGINT/SIGTERM and answers with _exit(130) after printing the stuck backtrace (this port's best diagnostic), producer_db_finish is not async-signal-safe, and the gate's 'timeout -s KILL' is SIGKILL, which no handler catches. MEASURED on a capped headless run (PSXPORT_NATIVE_FRAMES=2000): rc=0, the [producers] run-end line present, scratch/producers/run-2026-08-12T11:41:50.jsonl written. THE REPORT IS THE DESIGNED NEGATIVE, exactly as predicted: 0 rows, 0 claims, prims seen 1718994 = guest-origin 859497 + span-miss 859497, attributed 0, unscoped-native 0 — i.e. NOT MEASURED (parts (b) blind packet pool and (c) no ProducerScope), not 'this game draws nothing'. TWO DEFECTS FOUND ON THE WAY, both the same class as this issue — a lifecycle welded to a code path this port never enters: (1) a clean exit ABORTED (rc=139, 'terminate called without an active exception', std::thread::~thread() on the watchdog backtrace) because host_turn.cpp's file-scope timer std::thread is still joinable at exit; the framework already ships rec_host_turn_shutdown() (core.h) for exactly this and it had ZERO callers anywhere, because no psxport port had ever reached a clean exit at all — producer_run.cpp calls it now. (2) The config env audit prints NOTHING here, at boot or at exit: report_exit_audit is armed by psx::config::report_once(), whose only caller is cfg_dump(), whose only caller is native_boot.cpp:618, so it was never REGISTERED — not merely killed before printing, which is what its own comment claims. Gate: tools/gate.sh 40 PASSED on the first run, no spurious cold-cache failure.
